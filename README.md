# SudokuSolver
A Sudoku solver written in C++

## Build and run

```sh
cmake -S . -B build
cmake --build build
./build/SudokuSolver
```

The executable prints `Hello, World!` as the initial project setup.
